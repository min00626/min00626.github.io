---
date:   2026-03-31 15:01:35 +0300
title:  'Project R'
description: 각양각색의 컨텐츠를 제공하는 서브컬쳐 MMORPG입니다.
tags:   [Unity]
image:  '/images/reiw/reiw_thumbnail.png'
---

## 프로젝트 개요

Project R은 높은 커스터마이징 자유도와 다양한 컨텐츠를 제공하는 서브컬쳐 MMORPG입니다.

메인 스토리와 서브 퀘스트를 중심으로, 낚시와 같은 미니게임, PVE 및 PVP 등 다양한 플레이 경험을 제공합니다.

---

## 주요 작업

- **[ADV Tool](#adv-tool)**  
  대화 흐름과 연출을 그래프와 타임라인으로 제작할 수 있는 커스텀 에디터 툴을 개발하였습니다.

- **[낚시](#낚시)**  
  스테이트머신과 서버 동기화를 기반으로 동작하는 미니게임 컨텐츠를 구현하였습니다.

---

## ADV Tool

ADV 툴은  **대화문과, 대화문에 연결된 연출을 편집하기 위한 커스텀 에디터 툴**입니다. ADV 툴은 기능에 따라 크게 두 부분으로 나눌 수 있습니다.
1. **ADV 그래프**  
대화문을 그래프 형태로 편집합니다.
2. **ADV 타임라인**  
각 대화문에 연결된 연출을 편집합니다.


<br/>

### 시연 영상

{% include youtube.html id="u7sZGuiK19U" %}

<br/>

### ADV 그래프

<figure class="image-figure">
  <img src="/images/reiw/advgraph.png" alt="ADV Graph 구조도">
  <figcaption>ADV 그래프 예시</figcaption>
</figure>

GraphView API를 기반으로 대화문을 그래프 형태로 표현합니다.  
카메라 포커싱 및 오프셋, 캐릭터 페이셜 및 애니메이션 등의 연출을 사전에 정의하여 간편하게 적용할 수 있습니다.  
노드 자동 정렬, 오류 검출 등의 편의 기능을 제공하며, 다양한 단축키를 제공해 생산성을 높였습니다.

<br/>

#### 디자인 패턴

그래프는 MVP 패턴, 각각의 노드는 MVVM 패턴으로 구현하였습니다.
노드 위치나 엣지 연결 정보 등 그래프에 저장되는 데이터는 단순히 View가 전달한 데이터를 Model에 저장하기만 하는 구조이기 때문에 MVP를 사용하였습니다.
반면 각각의 노드는 Model의 변화에 View가 반응해야 하기 때문에 MVVM을 사용하였습니다.

<br/>

#### 데이터 모델 구조

ADV 그래프의 각 노드 모델은 개별 `ScriptableObject`로 구성되며, 그래프 모델의 서브애셋으로 관리됩니다.  
<div class="mermaid">
flowchart LR
    Graph[AdvGraphModel]

    subgraph Nodes[Sub Assets]
        Node1[AdvNodeModel]
        Node2[AdvNodeModel]
        Node3[AdvNodeModel]
    end

    Graph --> Nodes
</div>

이러한 구조를 채택한 이유는 다음과 같습니다.

1. **Undo 처리 단위 최적화**  
   `Undo.RecordObject` 호출 시 그래프 전체가 아닌 노드 단위로 기록할 수 있어, 에디터 비용이 감소하고 변경점을 직관적으로 기록할 수 있습니다.

2. **SerializedObject 기반 에디터 확장성**  
   각 노드 모델을 `SerializedObject`로 직접 다룰 수 있어, 프로퍼티 단위 편집과 노드 뷰 구현이 용이합니다.

3. **에디터 디버깅 및 가시성**  
   각 노드 모델을 인스펙터에서 직접 확인할 수 있어 디버깅과 유지보수가 용이합니다.

<br/>

#### 오류 검출

작업자의 편의를 위해, 그래프를 분석하여 오류를 검출하는 기능을 제공합니다. 오류는 심각도에 따라 `Error`와 `Warning`으로 구분됩니다.

<figure class="image-figure">
  <img src="/images/reiw/advgraph_validate.png" alt="ADV Graph 구조도">
  <figcaption>검출된 오류가 출력되는 모습</figcaption>
</figure>

이 기능은 오류를 가질 위험이 있는 일부 노드 모델이 IValidatableNode 인터페이스를 구현하는 방식으로 구현되었습니다. 오류 검출 시 노드 모델을 순회하며, `ErrorCode`와 에러가 발생한 노드의 GUID를 수집해 출력합니다.

```csharp
    public interface IValidatableNode
    {
        IEnumerable<ErrorCode> Validate();
    }
```

<br/>

### ADV 타임라인

ADV 그래프의 각 대화문 노드에는 타임라인 애셋이 연결되어 있습니다. 타임라인 애셋을 편집하여 대화문 출력 시 재생될 연출을 편집할 수 있습니다.

연출 편집을 위해 다양한 커스텀 트랙 및 마커를 제공합니다. ADV 타임라인에서 제공하는 기능은 다음과 같습니다.

1. 캐릭터 페이셜 및 애니메이션
2. 카메라 타게팅 및 애니메이션
3. 오브젝트 스폰 및 애니메이션
4. 페이드 인 / 아웃
5. BGM, SFX 재생 및 페이드
6. 포스트 프로세싱
7. 월페이퍼 연출

<br/>

#### 런타임 바인딩

타임라인 바인딩은 항상 런타임에, 대화문 재생이 시작될 때 수행됩니다.  
따라서 바인딩 대상이 모호한 타임라인 트랙의 경우, 커스텀 에디터를 통해 바인딩 대상을 지정합니다.

<figure class="image-figure">
  <img src="/images/reiw/advtimeline_binding.gif" alt="ADV Graph 구조도">
  <figcaption>NPC 애니메이션을 컨트롤하는 NpcAnimationTrack의 예시</figcaption>
</figure>

바인딩 대상은 트랙 애셋에 저장되며, 대화문 재생 시작 시 씬 내의 바인딩 대상을 찾아 바인딩합니다.

<br/>

#### 자동 바인딩

일부 트랙은 바인딩 대상이 명백합니다. 예를 들어, 카메라 애니메이션을 컨트롤하는 CameraControlTrack은 항상 ADV용 Cinemachine Virtual Camera에 바인딩됩니다.
바인딩 대상이 명백한 트랙은 생성 시 자동으로 바인딩됩니다.

<figure class="image-figure">
  <img src="/images/reiw/advtimeline_autobind.gif" alt="ADV Graph 구조도">
  <figcaption>CameraControlTrack 생성 시 자동 바인딩</figcaption>
</figure>

자동 바인딩 기능은 TrackEditor를 상속하고, OnCreate 함수를 override하여 구현하였습니다.

<div class="mermaid">
classDiagram
    class TrackEditor {
        +OnCreate()
    }

    class AutoBindTrackEditorBase {
        +OnCreate() override
        +ResolveBindingTarget()
    }

    class AutoBindCameraControlTrackEditor {
        +ResolveBindingTarget() override
    }

    class AutoBindFadeTrackEditor {
        +ResolveBindingTarget() override
    }

    class Etc {
        +ResolveBindingTarget() override
    }

    TrackEditor <|-- AutoBindTrackEditorBase
    AutoBindTrackEditorBase <|-- AutoBindCameraControlTrackEditor
    AutoBindTrackEditorBase <|-- AutoBindFadeTrackEditor
    AutoBindTrackEditorBase <|-- Etc
</div>

<br/>

### 코드 샘플

다음 코드는 AdvGraphPresenter가 그래프를 그릴 때 호출되는 함수입니다.  
그래프 초기화 시 InitializeGraph가 호출되고, 그래프에 변경점이 발생하면 OnGraphViewChanged 함수가 호출됩니다.
<details markdown="1">
<summary>코드 보기</summary>

```csharp
    public partial class AdvGraphPresenter 
    {
        // ...

        private void InitializeGraph()
        {
            isInitializing = true;
            
            groupMap.Clear();
            nodeMap.Clear();
            edgeMap.Clear();
            
            graphView.DeleteElements(graphView.graphElements);

            foreach (var group in graphModel.Groups)
            {
                var dataGraphGroup = new GroupView(this, group.Guid) { title = group.Title };
                dataGraphGroup.SetPosition(group.Rect);
                groupMap.Add(group.Guid, dataGraphGroup);
                graphView.AddElement(dataGraphGroup);
            }
            
            foreach (var node in graphModel.Nodes)
            {
                NodeViewBase editorNode = CreateNodeView(node);
                editorNode.SetPosition(node.Position);
                nodeMap[node.Guid] = editorNode;
                graphView.AddElement(editorNode);
                
                // 그룹에 속한 노드인 경우 그룹에 추가
                if (!string.IsNullOrEmpty(node.GroupId) && groupMap.TryGetValue(node.GroupId, out var group))
                {
                    group.AddElement(editorNode);
                }
            }
            
            foreach (var dataGraphEdge in graphModel.Edges)
            {
                var outputPort = GetPortFromEdge(dataGraphEdge.outputPort.nodeId, dataGraphEdge.outputPort.portId);
                var inputPort = GetPortFromEdge(dataGraphEdge.inputPort.nodeId, dataGraphEdge.inputPort.portId);
                if (outputPort != null && inputPort != null)
                {
                    var edge = outputPort.ConnectTo(inputPort);
                    edgeMap[edge] = dataGraphEdge;
                    graphView.AddElement(edge);
                }
            }
            
            CreateStartNode();
            
            isInitializing = false;
        }

        public GraphViewChange OnGraphViewChanged(GraphViewChange graphViewChange)
        {
            // Undo에서도 트리거되는데, Undo 중인 경우 무시
            if (Undo.isProcessing || isInitializing) return graphViewChange;
            
            if(graphViewChange.elementsToRemove != null)
            {
                var elements = graphViewChange.elementsToRemove;

                // 엣지를 먼저 삭제해야 엣지 정리 로직 수행 가능
                foreach (var edge in elements.OfType<Edge>())
                    RemoveEdge(edge);

                // 2순위 : 노드
                foreach (var node in elements.OfType<NodeViewBase>())
                    RemoveNode(node);

                // 3순위 : 그룹
                foreach (var group in elements.OfType<GroupView>())
                    RemoveGroup(group);
            }

            if (graphViewChange.movedElements != null)
            {
                foreach (var element in graphViewChange.movedElements)
                {
                    switch (element)
                    {
                        case NodeViewBase editorNode:
                            editorNode.Model.SetWithUndo(() =>
                            {
                                editorNode.Model.Position = editorNode.GetPosition();
                            }, "Node Moved");
                            break;
                        case GroupView group:
                        {
                            var groupData = graphModel.Groups.FirstOrDefault(g => g.Guid == group.Guid);
                            if (groupData != null)
                            {
                                graphModel.SetWithUndo(() =>
                                {
                                    groupData.Rect = group.GetPosition();
                                }, "Group Moved");
                            }

                            // 그룹에 속한 노드들의 위치도 업데이트
                            foreach (var editorNode in group.containedElements.OfType<NodeViewBase>())
                            {
                                editorNode.Model.SetWithUndo(() =>
                                {
                                    editorNode.Model.Position = editorNode.GetPosition();
                                }, "Node Moved");
                            }
                            break;
                        }
                    }
                }
            }

            if(graphViewChange.edgesToCreate != null)
            {
                foreach (Edge edge in graphViewChange.edgesToCreate)
                {
                    AddEdge(edge);
                }
            }
            
            SetAssetDirty();

            return graphViewChange;
        }
        
        // ...
    }
```        

</details>

---

## 낚시

낚시는 지정된 위치에서 시작할 수 있는 미니게임 컨텐츠입니다.  
스테이트머신 기반으로 작동하며, 진행 상황은 서버와 동기화됩니다.

<br/>

### 시연 영상

{% include youtube.html id="MiF13epRTSw" %}

<br/>

### 스테이트머신

스테이트머신의 구조는 다음과 같습니다.

<div class="mermaid">
stateDiagram-v2
    direction LR

    [*] --> Normal
    Normal --> Casting
    Casting --> Struggle
    Struggle --> Reeling

    Reeling --> Lift: Gauge Full
    Reeling --> Struggle: Gauge Not Full

    Lift --> Casting
</div>

현재 상태에 따라 캐릭터 애니메이션, UI 등이 변경되는 구조로 구현하였습니다.

<br/>

### 서버 동기화

플레이어 입력은 서버로 전달되어, 서버 검증 후 스테이트머신에 변경점이 반영됩니다.

<div class="mermaid">
sequenceDiagram
    participant P as Player
    participant C as Client
    participant S as Server

    P->>C: Player Input
    C->>S: Send Req
    S-->>C: Ack
    C->>C: Update State Machine
</div>

<br/>

### 코드 샘플

다음 코드 샘플은 낚시에서 사용된 스테이트 머신의 베이스 클래스입니다. 모든 스테이트는 FishingStateBase를 상속하여 구현됩니다.

<details markdown="1">
<summary>코드 보기</summary>

```csharp
private interface IFishingState
{
    ENUM_FISHING_STATE StateId { get; }
    void Enter(bool isInit);
    void Exit();
    void Tick(float dt);
    void Command(); // 플레이어 입력
}

private abstract class FishingStateBase : IFishingState
{
    public abstract ENUM_FISHING_STATE StateId { get; }

    protected readonly IngameFishingSystem Sys;
    protected FishingStateBase(IngameFishingSystem sys) => Sys = sys;

    public virtual void Enter(bool isInit) { }
    public virtual void Exit() { }

    public virtual void Tick(float dt)
    {
        // 공통 만료 타이머
        if (Sys.expiredTime > 0f)
        {
            Sys.expiredTime = Mathf.Max(0f, Sys.expiredTime - dt);
        }
    }

    public virtual void Command() { }
}
```

</details>
